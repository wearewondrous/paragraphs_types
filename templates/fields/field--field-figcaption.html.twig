{% for item in items %}
  {# check for text plain or text formatted #}
  {% if item.content['#value'] or item.content['#text'] %}
    <figcaption>
      {{ item.content }}
    </figcaption>
  {% endif %}
{% endfor %}
